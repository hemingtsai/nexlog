[project]
name = "nexlog-backend"
version = "0.1.0"
description = "A blog system backend made with Python and FastAPI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    
]

[tool.fastapi]
entrypoint = "main:app"